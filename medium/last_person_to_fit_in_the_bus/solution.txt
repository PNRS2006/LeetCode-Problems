/* Write your PL/SQL query statement below */
with cte as
(
    select person_id,person_name,weight,turn,
    sum(weight) over(order by turn) as wei
    from queue
)
select person_name from cte
where turn = (select max(turn) from cte where wei<=1000);