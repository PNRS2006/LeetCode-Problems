name: LeetCode Sync

on:
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Sync LeetCode Solutions
        uses: joshcai/leetcode-sync-action@main
        with:
          leetcode-username: 'UniqUe_Coderr_2006'
          github-token: ${{ secrets.GITHUB_TOKEN }}
          commit-message: 'feat: sync leetcode solution'
