# Write your MySQL query statement below
with act as
(select actor_id , director_id,count(*) as cnt
from ActorDirector 
group by actor_id, director_id
)
select actor_id,director_id from act where cnt>=3;